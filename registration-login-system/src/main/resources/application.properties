spring.application.name=registration-login-system
spring.datasource.url=jdbc:mysql://localhost:3306/registration_db?useSSL=false
spring.datasource.username=root
spring.datasource.password=root

#MySQLDialect does not need to be specified explicitly using 'hibernate.dialect' (remove the property setting and it will be selected by default)
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

#none, create-only, drop, create, create-drop, validate, update
#Creates automatically database tables based on dtos if they don't already exists
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.security.user.name=heltsu
spring.security.user.password=heltsu